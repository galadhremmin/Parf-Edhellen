<?php
  if (!defined('SYS_ACTIVE')) {
    exit;
  }
  
  class PageXp_headerController extends Controller {
    public function __construct(TemplateEngine &$engine) {
      // Don't do anything at the moment
    }
  }
